# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
.vscode/

# Native build artifacts (produced by scripts/build_native.ps1)
redimos_core.dll
redimos_core.so
redimos_core.dylib
native/redimos_core.dll
native/redimos_core.h
native/redimos_core.so
native/redimos_core.dylib

# OS
.DS_Store
Thumbs.db

# built redimos binaries
bin/

# packaged release artifacts (scripts/build.ps1)
dist/

# DynamoDB Local shared-db artifact (created when the java engine runs)
dynamodb-local-metadata.json

# lifeline janitor helper (built from native/janitor)
native/rm-janitor
rm-janitor
